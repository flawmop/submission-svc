apiVersion: v1
kind: Service
metadata:
  name: simulation-svc
  labels:
    app: simulation-svc
spec:
  type: ClusterIP
  selector:
    app: simulation-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9002
